services:
  web:
    build: .
    container_name: fixture_app
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - DB_PATH=/app/data/fixture.db
    volumes:
      - fixture_data:/app/data
    restart: unless-stopped

volumes:
  fixture_data:
